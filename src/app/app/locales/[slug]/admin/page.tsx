// Placeholder — relleno real en el Paso 5.
export default function AdminPlaceholder() {
  return (
    <div
      style={{
        maxWidth: 1100,
        marginTop: "2rem",
        padding: "3rem 2rem",
        textAlign: "center",
        border: "1px dashed #2a2a3a",
        borderRadius: 12,
        color: "#9ca3af",
      }}
    >
      <div style={{ fontSize: "2rem", marginBottom: "0.6rem" }}>⚙</div>
      <div style={{ color: "#cdcdd9", fontWeight: 600, marginBottom: "0.35rem" }}>
        Admin del local
      </div>
      <div style={{ fontSize: "0.9rem" }}>
        Editor de datos básicos del local. Pendiente (Paso 5).
      </div>
    </div>
  );
}
