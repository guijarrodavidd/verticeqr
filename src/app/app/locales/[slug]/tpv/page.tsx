// Placeholder — relleno real en el Paso 3.
export default function TpvPlaceholder() {
  return (
    <div
      style={{
        maxWidth: 1100,
        marginTop: "2rem",
        padding: "3rem 2rem",
        textAlign: "center",
        border: "1px dashed #2a2a3a",
        borderRadius: 12,
        color: "#9ca3af",
      }}
    >
      <div style={{ fontSize: "2rem", marginBottom: "0.6rem" }}>▣</div>
      <div style={{ color: "#cdcdd9", fontWeight: 600, marginBottom: "0.35rem" }}>
        TPV Sala
      </div>
      <div style={{ fontSize: "0.9rem" }}>
        Aquí irá el TPV de este local. Acoplamiento pendiente (Paso 3).
      </div>
    </div>
  );
}
