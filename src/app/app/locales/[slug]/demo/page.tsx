// Placeholder — relleno real en el Paso 4.
export default function DemoPlaceholder() {
  return (
    <div
      style={{
        maxWidth: 1100,
        marginTop: "2rem",
        padding: "3rem 2rem",
        textAlign: "center",
        border: "1px dashed #2a2a3a",
        borderRadius: 12,
        color: "#9ca3af",
      }}
    >
      <div style={{ fontSize: "2rem", marginBottom: "0.6rem" }}>✨</div>
      <div style={{ color: "#cdcdd9", fontWeight: 600, marginBottom: "0.35rem" }}>
        Demo visual del local
      </div>
      <div style={{ fontSize: "0.9rem" }}>
        Aquí va el mockup del menú QR según el sector. Pendiente (Paso 4).
      </div>
    </div>
  );
}
